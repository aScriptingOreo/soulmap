/**
 * This is a placeholder file to prevent build errors.
 * The drops feature has been replaced with an external link.
 */

export const loadDrops = async (): Promise<any> => ({});
export const findDropLocations = (): any[] => [];
